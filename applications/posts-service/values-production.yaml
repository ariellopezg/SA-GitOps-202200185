replicaCount: 1

image:
  tag: "0.0.0"

config:
  APP_VERSION: "0.0.0"
  LOG_LEVEL: info

hpa:
  minReplicas: 1
  maxReplicas: 2

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 300m
    memory: 320Mi
