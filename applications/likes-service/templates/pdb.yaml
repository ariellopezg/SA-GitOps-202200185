{{- if .Values.resilience.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "likes-service.fullname" . }}
  labels:
    {{- include "likes-service.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      {{- include "likes-service.selectorLabels" . | nindent 6 }}
{{- end }}
